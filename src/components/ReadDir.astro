---
import * as fs from 'fs';

const { dir } = Astro.props;
const assets = fs.readdirSync("./public/assets/" + dir + "/");
---

<h1 class="font-medium text-xl mb-10">
  <a class="text-blue-600 hover:underline hover:text-blue-400" href="/">assets</a> / {dir}
</h1>
<ul class="space-y-2">
  {assets.map((asset) => (
  <li class="p-2 rounded bg-gray-100 items-center">
    <a href={'/assets/' + dir + '/' + asset} target="_blank"
      class="text-blue-600 hover:underline hover:text-blue-400 flex-1">
      {asset}
    </a>
  </li>
  ))}
</ul>